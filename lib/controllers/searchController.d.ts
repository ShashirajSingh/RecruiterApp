import { Request, Response } from 'express';
export default class {
    static repoSearch(req: Request, res: Response): Promise<void>;
    static userSearch(req: Request, res: Response): Promise<any>;
}
